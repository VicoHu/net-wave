# 文件删除权限收归管理员

存储管理的删除端点原先仅校验节点身份，任意节点可删除任意文件（B 可删 A 发给 C 的文件），且文件列表对全员可见。决定：单个与批量删除仅管理员可执行；普通节点进入存储管理页时只读显示自己上传的文件（可下载）；「发错文件」的补救以发送前的上传确认弹窗承担（始终启用、不提供「不再询问」），而非授予上传者自删权——单一权限规则不出错，局域网内找管理员成本低。

## Consequences

- 发错文件后上传者自己无法删除，需管理员处理。
- `/storage` 同页双视角：服务端按管理员会话与节点身份返回不同数据范围。
