# 访问次数统一统计口径

文件的下载端点同时服务内联图片/视频预览与附件下载，技术上无法可靠区分「看了一眼」与「下载了文件」。决定：不区分，凡该端点**开始一次完整响应**即计一次「访问」（客户端中途取消亦计，不做流完成回调），字段、接口与界面统一用「访问」术语（访问次数 / 最后访问时间），不叫「下载次数」。

## Consequences

- 按此口径累积的数据在切换口径后不可比，属难逆转决策。
- 管理员经存储管理页下载文件同样计数，不设豁免。
