# TODOS

## 二期：WebRTC DataChannel 直连大文件传输

浏览器节点之间建立 DataChannel 直连，服务中心仅做信令交换；用于大文件场景，避免中心网卡中转开销（中转下中心收发各一遍，速度约减半）。

- [ ] 信令：复用中心实时通道交换 SDP/ICE
- [ ] 局域网 host candidates 直连（无需 STUN/TURN）
- [ ] 文件分片（DataChannel 单条消息 ≤256KB）与背压流控
- [ ] 直连建立失败时自动回退中转
- [ ] 浏览器兼容性验证（iOS Safari / Android Chrome / 桌面浏览器）

## 待办：中心访问口令

可选口令（环境变量设置），浏览器首次访问输入后 session 记住。用于公司 / 合租等不完全可信的局域网。MVP 默认完全开放，README 注明"仅限可信局域网使用"。

## 待办：房间口令

创建房间时可设置口令，加入需校验。MVP 房间公开可见、自由加入。
