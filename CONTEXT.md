# net-wave

局域网内点对点聊天与文件传输的 Web 应用。每个实例独立服务于一个局域网，实例之间不互联。

## Language

**服务中心（Hub）**:
运行 net-wave 服务进程的设备（PC / Mac / NAS / 树莓派），为节点提供 Web 入口，并中转消息与文件。
_Avoid_: 服务器、中心节点、master

**节点（Peer）**:
通过浏览器连接到某个服务中心的设备（手机 / 平板 / PC），免登录，以可修改的随机昵称标识。
_Avoid_: 客户端、用户、设备

**消息（Message）**:
会话中传输的一条内容，类型为文本、图片或文件；持久化存储，服务中心重启后可回看。
_Avoid_: 聊天记录条目

**中转（Relay）**:
消息与文件一律先上传至服务中心、再由对端下载的传输方式，区别于浏览器间直连。
_Avoid_: 转发、代理

**会话（Conversation）**:
两个节点之间（私聊）或一间房间的全部成员之间的持续消息流，含完整历史。
_Avoid_: 聊天窗、对话框

**房间（Room）**:
对中心内所有节点公开可见、可自由加入的多人会话；任意节点可创建，持久存在。
_Avoid_: 群组、群聊、频道

**隐藏（Hide）**:
把私聊会话从本人列表中移除，仅作用于本人视角：会话与历史仍在服务中心保留，对方不受影响；对方再来消息或本人再次发起私聊时恢复显示。
_Avoid_: 删除会话、关闭会话（「关闭」仅作为界面按钮文案）

**页内通知（In-page Notification）**:
页面可见时在页面内弹出的新消息横幅，无需浏览器授权，由本机偏好开关控制。
_Avoid_: 站内信、消息中心

**系统通知（System Notification）**:
经浏览器 Notification API 发出的操作系统级新消息通知；需要节点授权，且仅 secure context（localhost 或 HTTPS）下可用，默认 HTTP 部署时不可用。
_Avoid_: 推送（Push）、桌面通知

**管理员（Admin）**:
以密码登录管理中心的身份，负责房间与存储治理；与免登录的节点相对，一个服务中心仅一套管理员凭据。
_Avoid_: 站长、超级用户、root

**存储管理（Storage Management）**:
服务中心落盘文件的全量治理视角，仅管理员可删文件；普通节点进入同一页面时仅显示自己上传的文件，只读可下载。
_Avoid_: 文件管理、网盘

**访问次数（Access Count）**:
文件经下载端点被响应的累计次数（一次响应计一次），内联预览与附件下载不区分口径；伴生「最后访问时间」。
_Avoid_: 下载次数、浏览量

**上传确认（Upload Confirmation）**:
发送文件（含粘贴的图片/视频）前的二次确认弹窗，列出待发文件并可剔除；始终启用，不提供「不再询问」。
_Avoid_: 发送确认、撤回
